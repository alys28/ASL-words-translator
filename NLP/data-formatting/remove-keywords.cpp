#include <bits/stdc++.h>
using namespace std;
int main(){
    ifstream cin("tb-removed-keywords.in");
    string s;

    //*creating a set of all the articles in the english language
    unordered_set<string> articles;
    while(cin>>s){
        articles.insert(s);
    }

    //*looping through all the data sets, and removing the articles;
    ifstream cin();
    for()
}